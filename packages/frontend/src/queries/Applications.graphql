query Applications($org: String) {
    organization(username: $org) {
        id
        username
        applications {
            id
            name
            description
        }
        environments {
            id
            name
            label
        }
    }
}
