query Application($name: String!) {
    # Nested:
    organization(name: "foobar") {
        application(name: $name) {
            ...ApplicationFragment
        }
    }


    # Flat:
    application(organization: "foobar", name: $name) {
        ...ApplicationFragment
    }
}
