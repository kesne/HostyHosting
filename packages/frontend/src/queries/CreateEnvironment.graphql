mutation CreateEnvironment($org: ID, $name: String!, $label: String!) {
    organization(id: $org) {
        createEnvironment(name: $name, label: $label) {
            id
            name
            label
        }
    }
}
