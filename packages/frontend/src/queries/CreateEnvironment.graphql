mutation CreateEnvironment($org: Int, $name: String!, $label: String!) {
    organization(id: $org) {
        createEnvironment(name: $name, label: $label) {
            uuid
            name
            label
        }
    }
}
