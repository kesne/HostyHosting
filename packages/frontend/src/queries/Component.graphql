query Component($app: String!, $component: Int!) {
    application(name: $app) {
        id
        environments {
            id
            name
            label
        }
        component(id: $component) {
            ...ComponentFragment
        }
    }
}
