query Component($application: ID!, $component: ID!) {
    application(id: $application) {
        id
        environments {
            id
            name
            label
        }
        component(id: $component) {
            ...ComponentFragment
        }
    }
}
