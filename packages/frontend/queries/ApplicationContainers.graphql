query ApplicationContainers($id: Int!) {
    application(id: $id) {
        id
        containers {
            id
            size
            number
            deployment {
                id
                image
            }
        }
    }
}
